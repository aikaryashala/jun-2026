I have reverted the changes. As I made my students do the task6. They have asked few questions. I will describe those questions, add these question and answers at the end of the document. 1. I removed a semicolumn at the end of a C statement, and I am not able to debug and I am seeing an error. Why? 1A. Only when a program successfully compiles, we can debug the code. 2. In my case I got error but I am able to debug. 2A. The reason is earlier you have compiled succesfully, later introduced some compilation error in code. Compilation failed but earlier debug enabled executable still exists, so it feels like we are debugging new code, but in reality we are debugging earlier code. 3. What is lldb executable?   --- Write answers for these questions, improve the answers that are given here if not given write them, and add some diagrams to make them some questions' answer clearer.